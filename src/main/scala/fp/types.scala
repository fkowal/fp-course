package fp

class types {
  // domain Food: Vegetables, Meat, Fruit
  // codomain Feeling: Like, Love, Hate

  // create function likes that matches your eating preference
  //  Int = *
  //  * => *
  //  fn(a): b
  // List[A] = * => *, List[_]
  // 1. scala.collection.List
  // 2. F[_, _] = (*,*) => *, _[_,_]
  // 3. Option = * => *, _[_]
  // 4. Int = * = _
  // 5. T[_[_], _] = (* => *, *) => *, _[_[_], _]

  //  List[A], Option[A],

  //  val l: MyBox[String]
}
